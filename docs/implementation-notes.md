# Implementation notes

The product workflow is being implemented as an original Potolok Planner architecture. Public product descriptions were used to identify useful workflow patterns: drawing measurements, variants, catalog, estimates, projects, tasks, calendar and finance.

The current feature branch adds the domain foundation and a separate `/editor` route without replacing the stable home screen yet.
